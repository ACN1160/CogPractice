
services:
  web:
    build: .
    command: gunicorn --workers 2 --timeout 60 --bind 0.0.0.0:5000 wsgi:app
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
